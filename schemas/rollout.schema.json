{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RunConfig",
  "description": "Top-level run configuration.",
  "type": "object",
  "properties": {
    "algorithm": {
      "description": "Algorithm and its settings.",
      "$ref": "#/$defs/AlgorithmConfig"
    },
    "cloud": {
      "description": "Cloud provider selection (defaults to `local` so v1.0 TOMLs are unchanged).",
      "$ref": "#/$defs/CloudConfig",
      "default": {
        "provider": "local"
      }
    },
    "run": {
      "description": "Free-form metadata about the run.",
      "$ref": "#/$defs/RunMetadata",
      "default": {
        "name": null,
        "tags": []
      }
    },
    "schema_version": {
      "description": "Schema version. v1 refuses configs with version > 1.",
      "type": "integer",
      "format": "uint32",
      "default": 1,
      "maximum": 1,
      "minimum": 1
    },
    "storage": {
      "description": "Storage backend selection.",
      "$ref": "#/$defs/StorageConfig"
    }
  },
  "additionalProperties": false,
  "required": [
    "storage",
    "algorithm"
  ],
  "$defs": {
    "AlgorithmConfig": {
      "description": "Algorithm selection.",
      "oneOf": [
        {
          "description": "Supervised fine-tuning. References Phase-4 `training::SftSettings`.",
          "type": "object",
          "properties": {
            "base_model": {
              "description": "Base model to fine-tune.",
              "$ref": "#/$defs/ModelRef"
            },
            "budget": {
              "description": "Training budget.",
              "$ref": "#/$defs/TrainingBudget",
              "default": {
                "max_steps": null,
                "max_tokens": null,
                "max_walltime": null
              }
            },
            "dataset": {
              "description": "Training dataset.",
              "$ref": "#/$defs/DatasetRef"
            },
            "gradient_accumulation": {
              "description": "Gradient accumulation factor.",
              "type": "integer",
              "format": "uint32",
              "default": 1,
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "sft"
            },
            "loss_on": {
              "description": "Which tokens contribute to loss.",
              "$ref": "#/$defs/LossScope"
            },
            "minibatch_size": {
              "description": "Minibatch size (sequences).",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "optimizer": {
              "description": "Optimizer.",
              "$ref": "#/$defs/OptimizerSettings"
            },
            "packing": {
              "description": "Packing policy.",
              "$ref": "#/$defs/PackingPolicy"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "base_model",
            "optimizer",
            "dataset",
            "packing",
            "loss_on",
            "minibatch_size"
          ]
        },
        {
          "description": "Reward-model (Bradley-Terry) training. References Phase-4 `training::RmSettings`.",
          "type": "object",
          "properties": {
            "base_model": {
              "description": "Base model to head-tune.",
              "$ref": "#/$defs/ModelRef"
            },
            "budget": {
              "description": "Training budget.",
              "$ref": "#/$defs/TrainingBudget",
              "default": {
                "max_steps": null,
                "max_tokens": null,
                "max_walltime": null
              }
            },
            "dataset": {
              "description": "Pairwise dataset.",
              "$ref": "#/$defs/DatasetRef"
            },
            "head": {
              "description": "Head kind.",
              "$ref": "#/$defs/RmHeadKind"
            },
            "kind": {
              "type": "string",
              "const": "rm"
            },
            "minibatch_size": {
              "description": "Minibatch size (pairs).",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "optimizer": {
              "description": "Optimizer.",
              "$ref": "#/$defs/OptimizerSettings"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "base_model",
            "optimizer",
            "dataset",
            "head",
            "minibatch_size"
          ]
        },
        {
          "description": "Proximal policy optimization (Phase 9 placeholder).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "ppo"
            },
            "kl_coef_init": {
              "description": "Initial KL coefficient.",
              "type": [
                "number",
                "null"
              ],
              "format": "double",
              "default": null
            }
          },
          "additionalProperties": false,
          "required": [
            "kind"
          ]
        }
      ]
    },
    "AwsS3Config": {
      "description": "AWS S3 object-store settings.",
      "type": "object",
      "properties": {
        "bucket": {
          "description": "Target bucket name.",
          "type": "string"
        },
        "max_snapshot_part_bytes": {
          "description": "Hard cap on a single snapshot part (10 GiB ceiling).",
          "type": "integer",
          "format": "uint64",
          "default": 5368709120,
          "minimum": 0
        },
        "multipart_chunk_bytes": {
          "description": "Multipart upload chunk size in bytes (S3 minimum 5 MiB).",
          "type": "integer",
          "format": "uint64",
          "default": 16777216,
          "minimum": 0
        },
        "prefix": {
          "description": "Optional key prefix for all objects.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "bucket"
      ]
    },
    "AwsSecretsConfig": {
      "description": "AWS Secrets Manager settings.",
      "type": "object",
      "properties": {
        "allowlist": {
          "description": "Read-allowlisted secret names.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "AwsSqsConfig": {
      "description": "AWS SQS queue settings.",
      "type": "object",
      "properties": {
        "queue_url": {
          "description": "Fully-qualified SQS queue URL.",
          "type": "string"
        },
        "visibility_timeout_secs": {
          "description": "Visibility timeout in seconds (default 300).",
          "type": "integer",
          "format": "uint32",
          "default": 300,
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "queue_url"
      ]
    },
    "CloudConfig": {
      "description": "Cloud provider selection. `local` is the default so v1.0 TOMLs deserialize unchanged.",
      "oneOf": [
        {
          "description": "Local filesystem + in-memory backends (no cloud creds).",
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "const": "local"
            }
          },
          "required": [
            "provider"
          ]
        },
        {
          "description": "Amazon Web Services (S3 + SQS + Secrets Manager + EC2 `IMDSv2`).",
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "const": "aws"
            },
            "region": {
              "description": "AWS region (e.g., `us-west-2`).",
              "type": "string"
            },
            "s3": {
              "description": "S3 object-store settings.",
              "$ref": "#/$defs/AwsS3Config"
            },
            "secrets": {
              "description": "Secrets Manager settings.",
              "$ref": "#/$defs/AwsSecretsConfig",
              "default": {
                "allowlist": []
              }
            },
            "sqs": {
              "description": "SQS queue settings.",
              "$ref": "#/$defs/AwsSqsConfig"
            }
          },
          "additionalProperties": false,
          "required": [
            "provider",
            "region",
            "s3",
            "sqs"
          ]
        },
        {
          "description": "Google Cloud Platform (GCS + Pub/Sub + Secret Manager + GCE metadata).",
          "type": "object",
          "properties": {
            "gcs": {
              "description": "GCS object-store settings.",
              "$ref": "#/$defs/GcpGcsConfig"
            },
            "project": {
              "description": "GCP project id.",
              "type": "string"
            },
            "provider": {
              "type": "string",
              "const": "gcp"
            },
            "pubsub": {
              "description": "Pub/Sub queue settings.",
              "$ref": "#/$defs/GcpPubSubConfig"
            },
            "secrets": {
              "description": "Secret Manager settings.",
              "$ref": "#/$defs/GcpSecretsConfig",
              "default": {
                "allowlist": []
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "provider",
            "project",
            "gcs",
            "pubsub"
          ]
        }
      ]
    },
    "ContentId": {
      "description": "blake3 content hash; equality implies content equality.",
      "type": "string"
    },
    "DatasetRef": {
      "description": "Dataset reference. Phase 4 ships `JsonlPath` only; `Other` is enumerated\nfor forward compatibility (Phase 7 HF datasets Hub variant).",
      "oneOf": [
        {
          "description": "Path to a JSONL file on local disk.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "jsonl_path"
            },
            "path": {
              "description": "Filesystem path.",
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "path"
          ]
        },
        {
          "description": "Forward-compat variant; reading returns `Fatal(ConfigInvalid)` in Phase 4.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "other"
            }
          },
          "required": [
            "kind"
          ]
        }
      ]
    },
    "Duration": {
      "type": "object",
      "properties": {
        "nanos": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "secs": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "secs",
        "nanos"
      ]
    },
    "GcpGcsConfig": {
      "description": "GCP GCS object-store settings.",
      "type": "object",
      "properties": {
        "bucket": {
          "description": "Target GCS bucket.",
          "type": "string"
        },
        "max_snapshot_part_bytes": {
          "description": "Hard cap on a single snapshot part (10 GiB ceiling).",
          "type": "integer",
          "format": "uint64",
          "default": 5368709120,
          "minimum": 0
        },
        "prefix": {
          "description": "Optional object-name prefix.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "resumable_chunk_bytes": {
          "description": "Resumable-upload chunk size in bytes (5 MiB minimum).",
          "type": "integer",
          "format": "uint64",
          "default": 16777216,
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "bucket"
      ]
    },
    "GcpPubSubConfig": {
      "description": "GCP Pub/Sub queue settings.",
      "type": "object",
      "properties": {
        "ack_deadline_secs": {
          "description": "Ack deadline in seconds (default 30).",
          "type": "integer",
          "format": "uint32",
          "default": 30,
          "minimum": 0
        },
        "subscription": {
          "description": "Subscription id to pull from.",
          "type": "string"
        },
        "topic": {
          "description": "Topic id to publish to.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "subscription",
        "topic"
      ]
    },
    "GcpSecretsConfig": {
      "description": "GCP Secret Manager settings.",
      "type": "object",
      "properties": {
        "allowlist": {
          "description": "Read-allowlisted secret names.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "LossScope": {
      "description": "Selector for which tokens contribute to the loss in supervised training.",
      "oneOf": [
        {
          "description": "Mask loss to assistant-role spans (chat-template aware).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "assistant_only"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind"
          ]
        },
        {
          "description": "Compute loss across all tokens.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "full"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind"
          ]
        },
        {
          "description": "Custom mask specification (placeholder; Phase 7+).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "custom"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind"
          ]
        }
      ]
    },
    "LrSchedule": {
      "description": "Learning-rate schedule kind.",
      "oneOf": [
        {
          "description": "Constant LR.",
          "type": "string",
          "const": "constant"
        },
        {
          "description": "Linear warmup + linear decay.",
          "type": "string",
          "const": "linear"
        },
        {
          "description": "Linear warmup + cosine decay.",
          "type": "string",
          "const": "cosine"
        }
      ]
    },
    "ModelRef": {
      "description": "Reference to a model — `HuggingFace` ID, local path, or content-addressed URI.",
      "type": "object",
      "properties": {
        "content_id": {
          "description": "Optional content-addressed pin for reproducibility.",
          "anyOf": [
            {
              "$ref": "#/$defs/ContentId"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "tokenizer": {
          "description": "Tokenizer override; default uses the model's own.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "uri": {
          "description": "`HuggingFace` repo id, local path, or object-store URI.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "uri"
      ]
    },
    "OptimizerKind": {
      "description": "Optimizer kind discriminant.",
      "oneOf": [
        {
          "description": "`AdamW` (recommended).",
          "type": "string",
          "const": "adam_w"
        },
        {
          "description": "Adam (historical; rarely beats `AdamW`).",
          "type": "string",
          "const": "adam"
        },
        {
          "description": "Plain SGD (used by `MockBackend` tests).",
          "type": "string",
          "const": "sgd"
        }
      ]
    },
    "OptimizerSettings": {
      "description": "Optimizer settings.",
      "type": "object",
      "properties": {
        "betas": {
          "description": "`AdamW` betas; defaults to `(0.9, 0.999)`.",
          "type": "array",
          "default": [
            0.9,
            0.999
          ],
          "items": {
            "type": "number",
            "format": "double"
          },
          "maxItems": 2,
          "minItems": 2
        },
        "eps": {
          "description": "`AdamW` eps; default `1e-8`.",
          "type": "number",
          "format": "double",
          "default": 1e-8
        },
        "kind": {
          "description": "Optimizer kind.",
          "$ref": "#/$defs/OptimizerKind"
        },
        "lr": {
          "description": "Base learning rate.",
          "type": "number",
          "format": "double"
        },
        "schedule": {
          "description": "LR schedule.",
          "$ref": "#/$defs/LrSchedule",
          "default": "constant"
        },
        "warmup_steps": {
          "description": "LR warmup step count.",
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "weight_decay": {
          "description": "Weight decay coefficient.",
          "type": "number",
          "format": "double",
          "default": 0.0
        }
      },
      "additionalProperties": false,
      "required": [
        "kind",
        "lr"
      ]
    },
    "PackingKind": {
      "description": "Sequence packing kind.",
      "oneOf": [
        {
          "description": "Concatenate sequences up to `max_seq_len` with EOS separators.",
          "type": "string",
          "const": "concat"
        },
        {
          "description": "Bucketed packing (length-similar grouping). Phase 4 stub; Phase 9 finalises.",
          "type": "string",
          "const": "bucketed"
        },
        {
          "description": "No packing — one sequence per minibatch row.",
          "type": "string",
          "const": "off"
        }
      ]
    },
    "PackingPolicy": {
      "description": "Packing policy.",
      "type": "object",
      "properties": {
        "kind": {
          "description": "Packing kind.",
          "$ref": "#/$defs/PackingKind"
        },
        "max_seq_len": {
          "description": "Maximum packed sequence length (tokens).",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "kind",
        "max_seq_len"
      ]
    },
    "RmHeadKind": {
      "description": "Reward-model head kind.",
      "oneOf": [
        {
          "description": "Bradley-Terry pairwise comparison head.",
          "type": "string",
          "const": "bradley_terry"
        },
        {
          "description": "Pairwise logistic loss (alternative form).",
          "type": "string",
          "const": "pairwise_logistic"
        }
      ]
    },
    "RunMetadata": {
      "description": "Free-form run metadata; persisted but not interpreted by the framework.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Human-readable run name.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "tags": {
          "description": "Free-form tags.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "StorageConfig": {
      "description": "Storage backend selection.",
      "oneOf": [
        {
          "description": "Embedded local KV (sled/redb — choice locked in Phase 2).",
          "type": "object",
          "properties": {
            "backend": {
              "type": "string",
              "const": "embedded"
            },
            "path": {
              "description": "Filesystem path for the embedded DB.",
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "backend",
            "path"
          ]
        },
        {
          "description": "Postgres URL (lands in Phase 4 / TRAIN-04).",
          "type": "object",
          "properties": {
            "backend": {
              "type": "string",
              "const": "postgres"
            },
            "url": {
              "description": "Postgres connection URL.",
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "backend",
            "url"
          ]
        }
      ]
    },
    "TrainingBudget": {
      "description": "Training budget bounds.",
      "type": "object",
      "properties": {
        "max_steps": {
          "description": "Maximum number of optimizer steps.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "max_tokens": {
          "description": "Maximum tokens to consume.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "default": null,
          "minimum": 0
        },
        "max_walltime": {
          "description": "Maximum wall-clock duration.",
          "anyOf": [
            {
              "$ref": "#/$defs/Duration"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "additionalProperties": false
    }
  }
}
