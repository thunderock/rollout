//! `rollout-harness-text` (HARNESS-01) — text-completion `EnvHarness`.
//!
//! Wave-0 skeleton: registered workspace member built on the spec-07 trait
//! surface in `rollout-core`. The `TextCompletionEnv` impl + reward-via-plugin
//! path land in Wave-1 (plan 07-01).
#![forbid(unsafe_code)]

#[cfg(test)]
mod tests {
    #[test]
    fn skeleton_compiles() {}
}
