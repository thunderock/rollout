//! `rollout-plugin-host` — Phase-2 substrate crate; populated by plan 02-05.
//!
//! Wave-0 stub: this file exists so the workspace compiles and downstream
//! crates can declare a `path = "../rollout-plugin-host"` dependency. The
//! real `PluginHost` impl (cdylib + `PyO3` + sidecar modes) lands in plan 02-05.
