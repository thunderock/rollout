//! `rollout-cloud-local` — Phase-2 substrate crate; populated by plan 02-03.
//!
//! Wave-0 stub: this file exists so the workspace compiles and downstream
//! crates can declare a `path = "../rollout-cloud-local"` dependency. The
//! real FS object-store / in-mem queue / env secrets / /proc compute-hint
//! impls land in plan 02-03.
