//! `rollout-proto` — Phase-2 substrate crate; populated by plan 02-01.
//!
//! Wave-0 stub: this file exists so the workspace compiles and downstream
//! crates can declare a `path = "../rollout-proto"` dependency. The real
//! `tonic-build`-generated `transport.proto` + `plugin.proto` modules land
//! in plan 02-01.
