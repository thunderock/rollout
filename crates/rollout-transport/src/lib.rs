//! `rollout-transport` — Phase-2 substrate crate; populated by plan 02-04.
//!
//! Wave-0 stub: this file exists so the workspace compiles and downstream
//! crates can declare a `path = "../rollout-transport"` dependency. The
//! real tonic + rustls + (feature-flagged) tonic-h3 transport layer lands
//! in plan 02-04.
