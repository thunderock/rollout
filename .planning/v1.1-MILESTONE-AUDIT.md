---
milestone: v1.1
milestone_name: cloud + distribution + harnesses
audited: 2026-06-01
status: tech_debt
scores:
  requirements: 12/12
  phases: 3/3
  integration: 6/6
  flows: 2/2
gaps:
  requirements: []   # none unsatisfied
  integration: []    # all 6 cross-phase seams wired
  flows: []          # both E2E flows complete
nyquist:
  compliant_phases: [05]
  partial_phases: [06, 07]
  missing_phases: []
  overall: partial   # 06/07 have VALIDATION.md but nyquist_compliant:false (never run through validator)
tech_debt:
  - phase: 05-cloud-layer-object-store-snapshots
    items:
      - "RESOLVED in 05-08: workspace rustdoc intra-doc link + rustfmt drift in cloud-gcp/aws (no outstanding debt)."
  - phase: 06-multi-node-distribution
    items:
      - "Nyquist VALIDATION.md exists but nyquist_compliant:false — never run through /gsd:validate-phase."
  - phase: 07-harnesses-env-tool-eval
    items:
      - "Linux sandbox enforcement witnesses (seccomp/landlock/namespace negatives + per-tool happy paths) are #[cfg(target_os=linux)] — validate on the harness-linux CI lane, not macOS (by design, D-TOOL-05)."
      - "cargo doc --workspace --no-deps --all-features fails on upstream h3-quinn 0.0.7 (EXPERIMENTAL quic feature, private quinn::StreamId.0 / E0616). Pre-existing, independent of Phase 7. CI gate uses no --all-features and is green. Fix when the experimental QUIC transport is pursued (post-v1.1)."
      - "restart_no_duplicates.rs trips clippy (format_push_string, too_many_lines) under --features test-mock-backend. Pre-existing (plan 03-05), not in Phase-7 verify path. Fix in a 03-05 follow-up."
      - "Nyquist VALIDATION.md exists but nyquist_compliant:false — never run through /gsd:validate-phase."
  - phase: tooling
    items:
      - "gsd-tools resolves project root to the PRIMARY worktree (git-common-dir): state/roadmap/requirements writes from a linked worktree land in main .planning. ROADMAP plan-counter aggregation under-reports summary_count (phases 5/6 header checkboxes were never auto-ticked). Cosmetic; reconciled by hand for Phase 7."
  - phase: v1.0-carried
    items:
      - "9 items carried from the v1.0 audit (by-design human-gate / live-env GPU/HF/Docker paths + 1 latent Postgres scan_bytes wildcard divergence flagged for RL-01 in v1.2). See .planning/milestones/v1.0-MILESTONE-AUDIT.md."
---

# v1.1 Milestone Audit — cloud + distribution + harnesses

**Audited:** 2026-06-01 · **Status:** `tech_debt` (all requirements met, no blockers; accumulated deferred/validation items to review before archive)

> Audited from the `worktree-exec-phase-07` branch, which holds the complete v1.1 state (phases 5/6 from committed history, phase 7 from this branch). `gsd-tools` resolves to the main checkout, so this audit was assembled by reading worktree files directly.

## Definition of Done

> 3+ node setup runs `make smoke` against real AWS/GCP; spot-preempt signal triggers graceful drain. The algo-layer harness surface (env/tool/eval) that v1.2 PPO/GRPO consumes via trait objects is in place. No 7B training in v1.1 — RL phases stay v1.2.

**Met.** All 12 in-scope requirements satisfied; cross-phase wiring verified; both E2E flows trace cleanly in code. The live `make smoke-3node-{aws,gcp}` execution requires real cloud credentials and validates on CI lanes — the enabling code seams are all connected.

## Requirements Coverage — 12/12 satisfied

3-source cross-reference (REQUIREMENTS.md `[x]` · phase VERIFICATION.md `passed` · integration wiring confirmed in code):

| REQ | Phase | VERIFICATION | Traceability | Integration | Status |
|-----|-------|--------------|--------------|-------------|--------|
| CLOUD-01 | 5 | passed | [x] | S3/SQS/SecretsManager/IMDSv2 wired; `cloud doctor` exercises all four | ✓ satisfied |
| CLOUD-02 | 5 | passed | [x] | GCS/PubSub/SM/MDS wired | ✓ satisfied |
| CLOUD-03 | 5 | passed | [x] | `put_stream`/`get_stream` overridden in S3+GCS; streaming snapshot path | ✓ satisfied |
| CLOUD-04 | 5 | passed | [x] | `rollout cloud doctor` registered + dispatched | ✓ satisfied |
| DIST-01 | 6 | passed | [x] | `StorageLease` + epoch namespaces; lease CAS | ✓ satisfied |
| DIST-02 | 6 | passed | [x] | `WorkItemRecord` CAS + work-stealing | ✓ satisfied |
| DIST-03 | 6 | passed | [x] | `replay_and_serve` stateless-replayer; `coord_restart_no_duplicates` | ✓ satisfied |
| DIST-04 | 6 | passed | [x] | `drain()` consumes `preemption_signal`; `spot_drain_completes_within_lead_time` | ✓ satisfied |
| DIST-05 | 6 | passed | [x] | `fence_old_coordinator` + epoch rejection; `split_brain_old_coord_self_fences` | ✓ satisfied |
| HARNESS-01 | 7 | passed | [x] | `EnvHarness`→`TextCompletionEnv`; `env_deterministic_replay` | ✓ satisfied |
| HARNESS-02 | 7 | passed | [x] | `ToolHarness`→sandbox + 6 tools; Linux enforcement on CI lane | ✓ satisfied |
| HARNESS-03 | 7 | passed | [x] | `EvalHarness`→`BundledEval`; eval-as-job; `rollout eval` CLI | ✓ satisfied |

HARNESS-04 (eval gate) correctly deferred to v1.2 — not in v1.1 scope. No orphaned or unsatisfied requirements.

## Phase Verifications — 3/3 passed

| Phase | Plans | VERIFICATION | Notes |
|-------|-------|--------------|-------|
| 5 — Cloud layer + object-store snapshots | 8 | passed | localstack/fake-gcs emulator CI; `bit_identical_resume_via_{s3,gcs}` |
| 6 — Multi-node distribution | 5 | passed | lease/epoch fencing, work-stealing, stateless-replayer, spot-drain |
| 7 — Harnesses (env + tool + eval) | 6 | passed | spec-07 trait surface + 3 algo-layer crates; 4/4 SC |

## Cross-Phase Integration — 6/6 seams verified

1. **Phase 5 → 6:** Phase 6 consumes `ObjectStore::put_stream`/`get_stream`, `Queue::dequeue_with_lease`/`extend_lease`, `ComputeHint::preemption_signal` — live callers confirmed; AWS/GCP impls override them.
2. **Phase 6 → 7:** `rollout-harness-eval` (prod dep on `rollout-coordinator`) rides the `WorkItemRecord` CAS state machine — one example = one queue item.
3. **Phase 5 → 7:** `eval_reports` namespace registered in embedded storage + written by `BundledEval::run`.
4. **Trait surface:** all three harness crates implement the spec-07 traits; v1.0 `RewardModel` stub fully deleted; no duplicate trait defs.
5. **CLI:** `rollout cloud doctor` and top-level `rollout eval` both wired into clap; `rollout infer eval` removed from spec-08.
6. **Dependency direction:** 14/14 invariants hold; no harness crate has a production dep on a cloud crate.

## E2E Flows — 2/2 complete

- **Cloud run:** coordinator persists state (DIST-01) → lease-dequeue from cloud queue (CLOUD + DIST-02) → spot drain (DIST-04) → restart-replay (DIST-03). Connected end-to-end; live path on CI lanes.
- **Eval run:** `rollout eval --suite mmlu --checkpoint <id>` → eval-as-job on the work queue → `MockEvalBackend` scores offline fixtures (≤1% parity vs lm-eval v0.4.9) → `eval_reports` persist → per-task JSON. Verified GPU/network-free.

## Nyquist Compliance — partial

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|---------------|-------------------|--------|
| 5 | exists | true | — |
| 6 | exists | false | `/gsd:validate-phase 6` |
| 7 | exists | false | `/gsd:validate-phase 7` |

Phases 6 and 7 have validation scaffolding but were never run through the validator. Non-blocking, but recommended before milestone archive.

## Tech Debt Summary

No critical blockers. Accumulated items (see frontmatter `tech_debt`):
- **By-design / live-env:** Linux sandbox enforcement → `harness-linux` CI lane; `make smoke-3node` → live cloud creds; v1.0's 9 carried items (human-gate / GPU/HF/Docker).
- **Pre-existing / upstream:** `--all-features` rustdoc on `h3-quinn 0.0.7` (CI uses no `--all-features`, green); `restart_no_duplicates.rs` clippy under `test-mock-backend` (plan 03-05 follow-up).
- **Tooling hygiene:** gsd-tools project-root-resolves-to-main from worktrees; ROADMAP plan-counter aggregation under-reports (phases 5/6 header checkboxes); reconciled by hand.
- **Validation:** Nyquist PARTIAL on phases 6 & 7.

## Verdict

**v1.1 is functionally complete** — 12/12 requirements satisfied, 3/3 phases verified, 6/6 integration seams wired, 2/2 E2E flows trace cleanly. Status is `tech_debt` (not `passed`) only to surface the Nyquist-validation gap on phases 6/7 and the accumulated deferrals for review before archiving — none are blockers.
