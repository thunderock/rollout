{
  "mode": "interactive",
  "granularity": "standard",
  "parallelization": true,
  "commit_docs": false,
  "model_profile": "balanced",
  "workflow": {
    "research": true,
    "plan_check": true,
    "verifier": true,
    "nyquist_validation": true,
    "auto_advance": false,
    "research_before_questions": false
  },
  "notes": "commit_docs=false because user explicitly requested no commits during initialization. Planning artifacts live in .planning/ uncommitted; the user will decide when to commit. If commit_docs is flipped to true later, also remove .planning/ from .gitignore."
}
