---
phase: 07-harnesses-env-tool-eval
plan: 02
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - crates/rollout-harness-tool/Cargo.toml
  - crates/rollout-harness-tool/src/lib.rs
  - crates/rollout-harness-tool/src/sandbox/launcher.rs
  - crates/rollout-harness-tool/src/sandbox/seccomp.rs
  - crates/rollout-harness-tool/src/sandbox/cgroup.rs
  - crates/rollout-harness-tool/src/sandbox/capfs.rs
  - crates/rollout-harness-tool/src/sandbox/stub_macos.rs
  - crates/rollout-harness-tool/src/sandbox/mod.rs
  - crates/rollout-harness-tool/src/tools/python_exec.rs
  - crates/rollout-harness-tool/src/tools/shell.rs
  - crates/rollout-harness-tool/src/tools/file_read.rs
  - crates/rollout-harness-tool/src/tools/file_write.rs
  - crates/rollout-harness-tool/python/runner.py
  - crates/rollout-harness-tool/tests/sandbox_negative.rs
  - crates/rollout-harness-tool/tests/sandbox_positive.rs
  - crates/rollout-harness-tool/tests/macos_stub.rs
autonomous: true
requirements: [HARNESS-02]
must_haves:
  truths:
    - "On kernel <5.13 with require_landlock=true (default) the harness refuses to start (fail-closed, D-TOOL-02)"
    - "Forbidden syscalls (ptrace/mount/keyctl/bpf/socket/userns) return EPERM/error, not a crash (deny-default EPERM)"
    - "python_exec/shell run the allowlisted binary via execve (shell=False); file_read/file_write are confined to a cap-std tempdir root"
    - "On macOS the crate compiles and sandboxed invoke returns the documented dev-stub Fatal error"
  artifacts:
    - path: "crates/rollout-harness-tool/src/sandbox/seccomp.rs"
      provides: "curated ALLOWLIST const + per-syscall justification + filter builder"
      contains: "ALLOWLIST"
      min_lines: 40
    - path: "crates/rollout-harness-tool/src/sandbox/launcher.rs"
      provides: "layered namespace+rlimit+landlock+seccomp+cgroup exec launcher (correct ordering)"
      contains: "execve"
    - path: "crates/rollout-harness-tool/src/sandbox/stub_macos.rs"
      provides: "macOS compile-only dev stub returning the documented Fatal"
      contains: "sandbox unavailable on macOS"
  key_links:
    - from: "crates/rollout-harness-tool/src/tools/python_exec.rs"
      to: "src/sandbox/launcher.rs"
      via: "exec tool runs through the shared launcher"
      pattern: "launch|launcher"
    - from: "crates/rollout-harness-tool/src/sandbox/seccomp.rs"
      to: "seccompiler filter"
      via: "ALLOWLIST compiled to BPF, default ERRNO(EPERM)"
      pattern: "EPERM|Errno"
---

<objective>
Build the Linux sandbox core of `rollout-harness-tool` (HARNESS-02, part 1 of 2): the shared
layered launcher (rustix namespaces + setrlimit + landlock + seccompiler + cgroups v2), the
curated seccomp `ALLOWLIST` with per-syscall justification, the cap-std file-tool root, the
macOS compile-only dev stub, and the four non-HTTP tools (`python_exec`, `shell`, `file_read`,
`file_write`). Ships the full negative-test CVE matrix + positive witnesses. The HTTP tools +
SSRF connector + README sandbox-depth matrix land in 07-04.

Purpose: The highest-novelty, highest-risk work in the phase — a real (best-effort, honestly-
documented) process-isolation boundary. The only hand-rolled code is the composition ordering
and the curated allowlist; every primitive is a battle-tested crate.

Output: A compiling Linux sandbox + four tools + macOS stub; all SC2 enforcement witnesses green
on the Linux CI lane; the crate compiles + stub-errors on macOS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-harnesses-env-tool-eval/07-CONTEXT.md
@.planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md
@.planning/phases/07-harnesses-env-tool-eval/07-VALIDATION.md
@docs/specs/07-harnesses.md
@crates/rollout-core/src/traits/harness.rs
@crates/rollout-cloud-local/src/lib.rs
@scripts/check-forbidden-patterns.sh

<interfaces>
<!-- After 07-00. -->
```rust
#[async_trait] pub trait ToolHarness: Send + Sync {
    type Settings: DeserializeOwned + JsonSchema + Send + Sync + 'static;
    fn from_settings(settings, deps: HarnessDependencies) -> Result<Self, CoreError> where Self: Sized;
    fn descriptor(&self) -> ToolDescriptor;
    async fn invoke(&self, calls: Vec<ToolCall>) -> Result<Vec<ToolResult>, CoreError>;
}
pub enum SideEffectClass { Pure, Filesystem, Network, Exec }
pub enum ToolOutcome { Success, Error, TimedOut }
pub struct ToolCall { call_id: ToolCallId, tool: SmolStr, args: serde_json::Value, context: ToolContext }
pub struct ToolResult { call_id: ToolCallId, outcome: ToolOutcome, output: serde_json::Value, stderr: Option<String>, duration: Duration }
// CoreError::Fatal(FatalError::ConfigInvalid(String)) is the documented refuse/stub error.
```
Pins (07-00 [workspace.dependencies]): rustix =1.1.4, landlock =0.4.5, seccompiler =0.5.0, cap-std =4.0.2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curated seccomp ALLOWLIST + filter builder + negative-test matrix</name>
  <files>crates/rollout-harness-tool/src/sandbox/seccomp.rs, crates/rollout-harness-tool/src/sandbox/mod.rs, crates/rollout-harness-tool/tests/sandbox_negative.rs</files>
  <read_first>
    - .planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md "Curated seccomp allowlist (D-TOOL-07)" — the full syscall justification table + "Default action = SECCOMP_RET_ERRNO(EPERM)" + the explicit-deny list
    - .planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md "Open Questions #1" (clone3 flag-filtering limits → rely on no-new-privs + cap-drop + the userns negative test as load-bearing proof)
    - .planning/phases/07-harnesses-env-tool-eval/07-CONTEXT.md D-TOOL-07 (named negative fixtures) + D-TOOL-08 (threat boundary)
    - .planning/phases/07-harnesses-env-tool-eval/07-00-SUMMARY.md (the real strace baseline output captured by the 07-00 Linux CI lane — diff ALLOWLIST against it; expect 2-5 additions)
    - seccompiler 0.5.0 docs (filter builder API: SeccompFilter, SeccompRule, SeccompAction::Errno)
  </read_first>
  <behavior>
    - Test `seccomp_blocks_unexpected_syscall`: a child that issues `ptrace` returns EPERM (errno), the process does NOT segfault.
    - Test `sandbox_blocks_userns`: a child attempting `unshare(CLONE_NEWUSER)` / `clone3` with CLONE_NEWUSER fails (no further user namespace).
    - Test `sandbox_blocks_mount`: `mount`/`umount2` denied.
    - Test `sandbox_blocks_keyctl`: `keyctl`/`add_key`/`request_key` denied.
    - Test `sandbox_blocks_bpf`: `bpf` denied.
    - Test `seccomp_no_socket`: `socket()` denied for exec tools (deny-all net namespace + seccomp).
    - Test `tool_sandbox_escape_blocked`: an escape attempt (e.g. ptrace+execve a host binary) yields ToolResult outcome Error, not a host effect.
  </behavior>
  <action>
    `src/sandbox/seccomp.rs`: define `pub const ALLOWLIST: &[Syscall]` (or a builder returning the seccompiler
    `BpfProgram`) containing EXACTLY the RESEARCH table syscalls, EACH with a one-line `//` justification:
    `clone3`, `clone`, `openat2`, `openat`, `read`, `write`, `close`, `lseek`, `pread64`, `pwrite64`,
    `faccessat2`, `rseq`, `arch_prctl`, `set_robust_list`, `set_tid_address`, `futex`, `futex_waitv`,
    `prctl` (subset: PR_SET_NO_NEW_PRIVS/PR_SET_DUMPABLE/PR_GET_DUMPABLE/PR_SET_NAME/PR_CAPBSET_DROP),
    `rt_sigprocmask`, `rt_sigaction`, `rt_sigreturn`, `sigaltstack`, `pidfd_send_signal`, `mmap`, `munmap`,
    `mprotect`, `brk`, `madvise`, `mremap`, `exit`, `exit_group`, `getpid`, `gettid`, `getrandom`,
    `clock_gettime`, `nanosleep`, `getuid`, `geteuid`, `getgid`, `getegid`, `execve`, `execveat`,
    `getdents64`, `statx`, `newfstatat`, `fstat`, `readlinkat`, `epoll_create1`, `epoll_ctl`, `epoll_wait`,
    `pipe2`, `dup`, `dup3`, `fcntl`. Default action = `SeccompAction::Errno(EPERM)` (NOT Kill — so the tool
    returns a typed error + the harness can emit a `seccomp-violation` event, spec §7). Add a `// REFUSES CLONE_NEWUSER`
    comment on `clone3`/`clone` documenting the flag-filter limitation honestly (Open Question #1) — the load-bearing
    userns defense is `PR_SET_NO_NEW_PRIVS` + dropped `CAP_SYS_ADMIN` + the `sandbox_blocks_userns` test.

    Explicitly NOT in the allowlist (verified by negative tests): `ptrace`, `mount`, `umount2`, `keyctl`,
    `add_key`, `request_key`, `bpf`, `socket`, `connect`, `bind`, `unshare`/clone with CLONE_NEWUSER.

    `tests/sandbox_negative.rs`: all seven tests above, `#[cfg(target_os = "linux")]`, named EXACTLY as in
    07-VALIDATION.md. On macOS these compile out (the macos_stub test in Task 4 covers macOS). After landing,
    diff `ALLOWLIST` against the 07-00 strace baseline; add the 2-5 surfaced syscalls (RESEARCH MEDIUM-confidence
    note) so `seccomp_python_runs` (Task 3) passes.
  </action>
  <verify>
    <automated>cargo build -p rollout-harness-tool --all-features &amp;&amp; cargo test -p rollout-harness-tool seccomp_blocks_unexpected_syscall sandbox_blocks_userns sandbox_blocks_mount sandbox_blocks_keyctl sandbox_blocks_bpf seccomp_no_socket tool_sandbox_escape_blocked --tests --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `crates/rollout-harness-tool/src/sandbox/seccomp.rs` contains `clone3` AND a comment refusing `CLONE_NEWUSER`.
    - `rg "ALLOWLIST" crates/rollout-harness-tool/src/sandbox/seccomp.rs` matches; the file contains `rseq`, `arch_prctl`, `openat2`, `faccessat2` (the post-2020 syscalls).
    - `rg "Errno|EPERM" crates/rollout-harness-tool/src/sandbox/seccomp.rs` matches (default action is EPERM, not Kill).
    - On the Linux lane: `cargo test -p rollout-harness-tool seccomp_blocks_unexpected_syscall sandbox_blocks_userns sandbox_blocks_mount sandbox_blocks_keyctl sandbox_blocks_bpf seccomp_no_socket` all exit 0.
    - `cargo test -p rollout-harness-tool tool_sandbox_escape_blocked` exits 0 on Linux.
  </acceptance_criteria>
  <done>The curated seccomp allowlist compiles to a deny-default-EPERM BPF filter; every named CVE-class negative test passes on the Linux lane.</done>
</task>

<task type="auto">
  <name>Task 2: Layered launcher (namespaces + rlimits + landlock + cgroups) + fail-closed kernel gate + cap-std file root</name>
  <files>crates/rollout-harness-tool/src/sandbox/launcher.rs, crates/rollout-harness-tool/src/sandbox/cgroup.rs, crates/rollout-harness-tool/src/sandbox/capfs.rs, crates/rollout-harness-tool/src/lib.rs, crates/rollout-harness-tool/Cargo.toml</files>
  <read_first>
    - .planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md "Pattern 3: Layered sandbox launcher — EXACT ordering" (the load-bearing PARENT/CHILD step sequence; seccomp LAST) + "The fail-closed kernel gate (D-TOOL-02)" + "cgroups v2 delegation plumbing (D-TOOL-04)"
    - .planning/phases/07-harnesses-env-tool-eval/07-CONTEXT.md D-TOOL-01 (layered defense), D-TOOL-02 (kernel ≥5.13 fail-closed), D-TOOL-04 (rlimits + cgroups), D-TOOL-06 (exact-full-path allowlist, shell=False)
    - .planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md "Open Questions #4" (cgroups degrade-with-warning, NOT fail-closed — confirmed approach) + Pitfall B
    - crates/rollout-cloud-local/src/lib.rs (the Linux-full / macOS-minimal cfg dual-impl precedent — ComputeHint)
    - landlock 0.4.5 docs (ABI enum for feature detection); rustix 1.1.4 process module (setrlimit, namespace flags)
  </read_first>
  <action>
    `src/lib.rs` `from_settings`: implement the fail-closed kernel gate (D-TOOL-02). Read kernel version via
    rustix `uname()`. If `< 5.13` AND `settings.require_landlock` (default `true`), return
    `CoreError::Fatal(FatalError::ConfigInvalid("landlock requires kernel >= 5.13, found {n}".into()))`.
    Operators set `require_landlock = false` to run with reduced isolation. Use the `landlock::ABI` enum to detect
    supported ABI rather than hardcoding kernel→feature maps. Add a `ToolSettings` deriving `Deserialize + JsonSchema`
    with `require_landlock: bool` (default true), the per-tool enable flags, the exec binary allowlist
    (`python_path: PathBuf` etc. resolved to EXACT full paths at init — `/usr/bin/python3`, NOT `python3` — D-TOOL-06),
    rlimit values, and the cgroup memory/pids limits.

    `src/sandbox/launcher.rs`: the shared exec launcher implementing RESEARCH Pattern 3 ordering EXACTLY:
    PARENT (1) kernel gate (2) resolve binary to exact full path (3) per-invocation cap-std tempdir (4) cgroup
    subtree write pids.max/memory.max — degrade-with-warning if no delegated tree (Open Question #4, Pitfall B; emit
    a warning event, rely on rlimits) (5) spawn child via clone3 with
    `CLONE_NEWUSER|CLONE_NEWPID|CLONE_NEWNET|CLONE_NEWNS|CLONE_NEWUTS|CLONE_NEWIPC` (net namespace = deny network).
    CHILD pre-execve, in this exact order: (6) join cgroup (7) `rustix::process::setrlimit` RLIMIT_CPU/AS/NOFILE/NPROC
    (8) landlock ruleset {tempdir rw, binary + lib deps ro} + enforce (9) install the seccomp filter from Task 1
    (default ERRNO(EPERM)) (10) `execve(exact_path, argv_vector, minimal_env)` — argv vector, NEVER `/bin/sh -c`.
    Document WHY seccomp is LAST (it must not block its own landlock/rlimit setup syscalls).

    `src/sandbox/cgroup.rs`: detect a writable delegated v2 tree (`cgroup.subtree_control` writable); write
    `memory.max` + `pids.max`; on absence emit warning + skip (rlimit fallback). `src/sandbox/capfs.rs`: a cap-std
    `Dir::open_ambient_dir`/rooted `Dir` at the per-invocation tempdir for file tools (rejects `..`/symlink by
    construction — PITFALLS 10b); add canonicalize-then-assert-prefix belt-and-suspenders.

    `Cargo.toml`: add the sandbox deps Linux-gated:
    `[target.'cfg(target_os = "linux")'.dependencies] rustix.workspace=true; landlock.workspace=true; seccompiler.workspace=true; cap-std.workspace=true; tempfile.workspace=true`. Add the per-crate
    `[lints.rust] unsafe_code = "deny"` + a file-level `#[allow(unsafe_code)]` only at the launcher boundary
    (Phase-2 plugin-host precedent — the workspace `forbid` can't be relaxed at file level). NEVER `libc::fork(`
    (forbidden-patterns grep) — use clone3 via rustix / `Command::spawn` (execve), per Pitfall F / PITFALLS 13.
  </action>
  <verify>
    <automated>cargo build -p rollout-harness-tool --all-features &amp;&amp; cargo clippy -p rollout-harness-tool --all-targets --all-features -- -D warnings &amp;&amp; bash scripts/check-forbidden-patterns.sh &amp;&amp; RUSTDOCFLAGS="-D warnings -D rustdoc::broken_intra_doc_links -D rustdoc::missing_crate_level_docs" cargo doc -p rollout-harness-tool --no-deps --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `crates/rollout-harness-tool/src/lib.rs` contains the exact string `landlock requires kernel >= 5.13` (fail-closed gate, D-TOOL-02).
    - `rg "require_landlock" crates/rollout-harness-tool/src/lib.rs` matches with a default of `true`.
    - `crates/rollout-harness-tool/src/sandbox/launcher.rs` contains `execve` AND a comment explaining seccomp is installed LAST.
    - `rg "CLONE_NEWNET|CLONE_NEWUSER|CLONE_NEWPID" crates/rollout-harness-tool/src/sandbox/launcher.rs` matches (namespace set).
    - `rg "libc::fork\(" crates/rollout-harness-tool` returns NOTHING; `bash scripts/check-forbidden-patterns.sh` exits 0.
    - `crates/rollout-harness-tool/Cargo.toml` contains `cfg(target_os = "linux")` gating the sandbox deps.
    - `cargo clippy -p rollout-harness-tool --all-targets --all-features -- -D warnings` exits 0.
    - The RUSTDOCFLAGS-deny `cargo doc -p rollout-harness-tool` command exits 0 (DOCS-03 — the launcher/seccomp/ToolSettings public types are documented).
  </acceptance_criteria>
  <done>The layered launcher composes namespaces+rlimits+landlock+seccomp+cgroups in the load-bearing order; the kernel gate fails closed; cgroups degrade-with-warning; file tools use a cap-std root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The four non-HTTP tools (python_exec, shell, file_read, file_write) + positive witnesses</name>
  <files>crates/rollout-harness-tool/src/tools/python_exec.rs, crates/rollout-harness-tool/src/tools/shell.rs, crates/rollout-harness-tool/src/tools/file_read.rs, crates/rollout-harness-tool/src/tools/file_write.rs, crates/rollout-harness-tool/python/runner.py, crates/rollout-harness-tool/src/lib.rs, crates/rollout-harness-tool/tests/sandbox_positive.rs</files>
  <read_first>
    - docs/specs/07-harnesses.md §3 "Bundled tools" table (side-effect class + sandbox per tool) + "Local-test parity" (happy path + ≥1 failure mode each)
    - .planning/phases/07-harnesses-env-tool-eval/07-CONTEXT.md D-TOOL-03 (six tools, each feature-gated, happy + ≥1 failure test), D-TOOL-06 (subprocess shell=False, exact-full-path, per-invocation tempdir, NOT in-process PyO3)
    - .planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md "Anti-Patterns to Avoid" (shell=True ban; cap-std for path traversal; subprocess not libc::fork)
    - crates/rollout-plugin-host/python/ (the stdlib-only sidecar pattern — AF_UNIX framed JSON, NO pip install per AGENTS.md §7 — for the python runner)
    - crates/rollout-harness-tool/src/sandbox/launcher.rs + capfs.rs (from Task 2 — exec tools call the launcher; file tools use the cap-std root)
  </read_first>
  <behavior>
    - Test `seccomp_python_runs` (positive — validates the allowlist): `python_exec` running `/usr/bin/python3 -c 'print(1)'`
      under the full sandbox returns outcome Success with stdout `1` (proves the allowlist is complete — this is the
      automated proxy for the real strace spike).
    - Test `python_exec_*`: a happy path + ≥1 failure mode (timeout → ToolOutcome::TimedOut; per-call timer, spec §7).
    - Test `shell_*`: allowlisted command runs; a non-allowlisted command is refused; argv vector (no `/bin/sh -c`).
    - Test `file_read_*` / `file_write_*`: read/write within the cap-std tempdir succeeds; a `..`/symlink escape is rejected (cap-std by construction).
  </behavior>
  <action>
    Each tool feature-gated in `Cargo.toml` (`[features] python_exec=[], shell=[], file_read=[], file_write=[]`,
    plus a `default`/`all-tools` aggregate so CI exercises all). Each is a small `ToolHarness`-ish module the parent
    crate's `ToolHarness::invoke` dispatches to by `ToolCall.tool` name, returning a `ToolResult` (outcome/output/
    stderr/duration). `descriptor()` lists the enabled tools' `ToolSpec` (name, description, input_schema, side_effects,
    timeout).

    `python_exec` + `shell` (exec, SideEffectClass::Exec): build an argv vector (NEVER a shell string), resolve the
    binary to its exact full path from the allowlist (D-TOOL-06), run through the Task-2 launcher (namespaces+seccomp+
    landlock+rlimits+cgroup, deny-network). Per-call timeout from `ToolSpec.timeout` → `ToolOutcome::TimedOut`.
    `python/runner.py` is a stdlib-only helper (NO third-party imports, NO `subprocess(shell=True)` — forbidden-patterns
    grep; NO pip install — AGENTS.md §7); used only if a sidecar runner is needed, otherwise exec python3 directly with
    `-c`. `file_read` + `file_write` (SideEffectClass::Filesystem): in-process via the cap-std `Dir` root at the
    per-invocation tempdir — no subprocess; reject path-escape.

    Add `tests/sandbox_positive.rs` with `seccomp_python_runs` + per-tool happy + ≥1 failure test, all
    `#[cfg(target_os = "linux")]`. If `seccomp_python_runs` fails because of a missing syscall, add it to Task-1
    `ALLOWLIST` with a justification (this is the strace-spike close-the-loop step).
  </action>
  <verify>
    <automated>cargo test -p rollout-harness-tool seccomp_python_runs python_exec shell file_read file_write --tests --all-features &amp;&amp; bash scripts/check-forbidden-patterns.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg "shell=True" crates/rollout-harness-tool` returns NOTHING; `rg "/bin/sh" crates/rollout-harness-tool/src/tools` returns NOTHING (argv vector only, D-TOOL-06).
    - `crates/rollout-harness-tool/Cargo.toml` has `[features]` with `python_exec`, `shell`, `file_read`, `file_write`.
    - On the Linux lane: `cargo test -p rollout-harness-tool seccomp_python_runs` exits 0 (allowlist validated — positive proxy for the strace spike).
    - `cargo test -p rollout-harness-tool python_exec shell file_read file_write --all-features` exits 0 on Linux (each tool: happy + ≥1 failure).
    - `bash scripts/check-forbidden-patterns.sh` exits 0 (no shell=True / libc::fork in the python or rust paths).
  </acceptance_criteria>
  <done>All four non-HTTP tools work within their allowlists (positive witnesses green) with happy + failure coverage; python runs under seccomp proving the allowlist; no shell=True / in-process PyO3.</done>
</task>

<task type="auto">
  <name>Task 4: macOS compile-only dev stub</name>
  <files>crates/rollout-harness-tool/src/sandbox/stub_macos.rs, crates/rollout-harness-tool/src/sandbox/mod.rs, crates/rollout-harness-tool/tests/macos_stub.rs</files>
  <read_first>
    - .planning/phases/07-harnesses-env-tool-eval/07-CONTEXT.md D-TOOL-05 (macOS = compile-only stub; sandboxed invoke returns Fatal::ConfigInvalid("sandbox unavailable on macOS — dev stub"); NO unsandboxed-run flag)
    - .planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md "macOS dev stub (D-TOOL-05)" (cfg(not(target_os = "linux")) whole sandbox module to a stub)
    - crates/rollout-cloud-local/src/lib.rs (the Linux-full / macOS-minimal cfg dual-impl precedent)
  </read_first>
  <action>
    `src/sandbox/mod.rs`: `#[cfg(target_os = "linux")] mod launcher; #[cfg(target_os = "linux")] mod seccomp; ...`
    and `#[cfg(not(target_os = "linux"))] mod stub_macos;` re-exporting the stub's launcher under the same name so
    `lib.rs` compiles unchanged on both platforms. `src/sandbox/stub_macos.rs`: the launcher entry returns
    `Err(CoreError::Fatal(FatalError::ConfigInvalid("sandbox unavailable on macOS — dev stub".into())))` verbatim
    — NO `allow_unsandboxed` flag (rejected, D-TOOL-05). The `from_settings` kernel gate is also stubbed (no uname on
    macOS) so construction succeeds but `invoke` returns the stub error.

    `tests/macos_stub.rs`: `#[cfg(not(target_os = "linux"))]` test asserting an `invoke` of any exec tool returns the
    exact documented Fatal string. On Linux this test compiles out.
  </action>
  <verify>
    <automated>cargo build -p rollout-harness-tool --all-features &amp;&amp; cargo test -p rollout-harness-tool macos_stub --tests --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `crates/rollout-harness-tool/src/sandbox/stub_macos.rs` contains the exact string `sandbox unavailable on macOS — dev stub`.
    - `rg "allow_unsandboxed" crates/rollout-harness-tool` returns NOTHING (D-TOOL-05 — no dev escape hatch).
    - `cargo build -p rollout-harness-tool --all-features` succeeds on macOS.
    - On macOS: `cargo test -p rollout-harness-tool macos_stub` exits 0 (stub returns the documented Fatal).
  </acceptance_criteria>
  <done>The crate compiles on macOS and sandboxed invoke returns the documented dev-stub Fatal; no unsandboxed escape hatch.</done>
</task>

</tasks>

<verification>
- macOS lane: `cargo build -p rollout-harness-tool --all-features` + `cargo test -p rollout-harness-tool macos_stub` green.
- Linux lane: all negative (`sandbox_blocks_*`, `seccomp_*`, `tool_sandbox_escape_blocked`) + positive (`seccomp_python_runs`, per-tool) tests green.
- `cargo clippy -p rollout-harness-tool --all-targets --all-features -- -D warnings` green.
- `bash scripts/check-forbidden-patterns.sh` green.
</verification>

<success_criteria>
- Layered defense composes in the correct order; kernel gate fails closed (D-TOOL-02); cgroups degrade-with-warning.
- Curated seccomp ALLOWLIST with per-syscall justification; deny-default EPERM; every CVE-class negative witness green (SC2).
- Four non-HTTP tools work within allowlists; python runs under seccomp; no shell=True / in-process PyO3 (D-TOOL-06).
- macOS compiles to the documented dev stub (D-TOOL-05).
</success_criteria>

<output>
After completion, create `.planning/phases/07-harnesses-env-tool-eval/07-02-SUMMARY.md` recording: the FINAL
seccomp allowlist (including any syscalls added after diffing the 07-00 strace baseline), the cgroup
degrade-with-warning decision as implemented, the exact ToolSettings shape, and any clone3 flag-filter
limitation discovered on the Linux runner (Open Question #1).
</output>
