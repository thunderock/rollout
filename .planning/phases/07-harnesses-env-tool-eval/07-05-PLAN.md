---
phase: 07-harnesses-env-tool-eval
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-03", "07-04"]
files_modified:
  - crates/rollout-cli/src/main.rs
  - crates/rollout-cli/src/eval.rs
  - crates/rollout-cli/Cargo.toml
  - crates/rollout-cli/tests/eval_cli.rs
  - docs/specs/08-cli.md
  - docs/book/src/SUMMARY.md
  - docs/book/src/harnesses/index.md
  - docs/book/src/harnesses/env.md
  - docs/book/src/harnesses/tool-sandbox.md
  - docs/book/src/harnesses/eval.md
  - docs/book/src/harnesses/cli.md
autonomous: true
requirements: [HARNESS-01, HARNESS-02, HARNESS-03]
must_haves:
  truths:
    - "rollout eval --suite mmlu --checkpoint <snapshot-id> dispatches to the eval harness and produces per-task scores"
    - "spec 08 is reconciled: rollout infer eval removed, rollout eval added top-level (D-EVAL-02)"
    - "mdBook ships a Harnesses chapter (env, tool-sandbox, eval, CLI) and builds clean"
    - "cargo test --workspace --tests is green with the 5 new crates; dep-direction lint is at 14 invariants (SC4)"
  artifacts:
    - path: "crates/rollout-cli/src/eval.rs"
      provides: "rollout eval subcommand (clap surface + checkpoint→ModelRef resolution + dry-run)"
      contains: "suite"
    - path: "docs/book/src/harnesses/tool-sandbox.md"
      provides: "tool sandbox mdBook chapter (depth matrix, threat boundary)"
      contains: "seccomp"
  key_links:
    - from: "crates/rollout-cli/src/main.rs"
      to: "Eval(EvalCmd) arm in enum Cmd"
      via: "top-level subcommand sibling to Infer/Train/Snapshot"
      pattern: "Eval"
    - from: "crates/rollout-cli/src/eval.rs"
      to: "rollout-harness-eval EvalHarness::run"
      via: "checkpoint→ModelRef→run dispatch"
      pattern: "EvalHarness|run\\("
---

<objective>
Phase closeout. Wire the top-level `rollout eval` CLI (D-EVAL-02) onto `rollout-cli`, mirroring the
`rollout infer batch` clap surface (dry-run, feature-gated backend, checkpoint→ModelRef resolution).
Reconcile `docs/specs/08-cli.md` (remove `rollout infer eval`, add top-level `rollout eval`). Ship the
mdBook Harnesses chapter (env, tool-sandbox, eval, CLI). Verify the ROADMAP SC4 gate: `cargo test
--workspace --tests` green with the 5 new crates and the dep-direction lint at 14 invariants.

Purpose: Make the three harness crates usable end-to-end from the CLI, align the spec to the shipped
shape, document the phase, and prove the workspace-count + invariant-count gate.

Output: `rollout eval` CLI + spec-08 edit + mdBook chapters + the SC4 verification green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-harnesses-env-tool-eval/07-CONTEXT.md
@.planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md
@.planning/phases/07-harnesses-env-tool-eval/07-VALIDATION.md
@docs/specs/07-harnesses.md
@docs/specs/08-cli.md
@docs/specs/04-storage-snapshots.md
@crates/rollout-cli/src/main.rs
@crates/rollout-cli/src/infer.rs
@crates/rollout-core/tests/dependency_direction.rs

<interfaces>
<!-- After 07-00..04. -->
```rust
// rollout-cli enum Cmd (main.rs): Schema, Worker, Coordinator, Infer(InferCmd), Train(TrainCmd),
//   Snapshot(SnapshotCmd), Cloud(CloudCmd) — add Eval(EvalCmd) as a sibling.
// Checkpoint resolution (snapshot.rs / spec 04): SnapshotId → Snapshot.parts[] (role "tar") →
//   the weights blob's ContentId populates ModelRef.content_id.
pub struct ModelRef { uri: String, content_id: Option<ContentId>, tokenizer: Option<String> }
// rollout-harness-eval: EvalHarness::run(model: ModelRef, ctx: EvalContext) -> EvalReport (07-03).
```
infer.rs clap pattern: `#[derive(Args)] InferCmd { #[command(subcommand)] sub: InferSub }`;
BatchArgs has `--config`, `--resume`, `--dry-run`; backend feature-gated (`vllm` / `test-mock-backend`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rollout eval CLI subcommand + spec-08 reconciliation</name>
  <files>crates/rollout-cli/src/main.rs, crates/rollout-cli/src/eval.rs, crates/rollout-cli/Cargo.toml, crates/rollout-cli/tests/eval_cli.rs, docs/specs/08-cli.md</files>
  <read_first>
    - crates/rollout-cli/src/main.rs (the `enum Cmd` to extend + the dispatch match)
    - crates/rollout-cli/src/infer.rs (the clap Args/Subcommand surface to mirror: --config, --dry-run, three-tier run-id, feature-gated backend selection)
    - crates/rollout-cli/src/snapshot.rs (the SnapshotId→Snapshot resolution path for --checkpoint → ModelRef.content_id, per spec 04)
    - .planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md "Pattern 6: rollout eval CLI (D-EVAL-02) — reconcile spec 08" (the exact CLI surface + the spec-08 edit instructions + the v1.2 "Harness DAG" footnote)
    - .planning/phases/07-harnesses-env-tool-eval/07-CONTEXT.md D-EVAL-02 (top-level rollout eval; reconcile spec 08), D-CORE-02 (DAG validation is v1.2 — leave a TODO/footnote, do NOT implement)
    - docs/specs/08-cli.md §"rollout infer <mode>" (line ~151, the `rollout infer eval` form to remove)
  </read_first>
  <behavior>
    - Test (dry-run, no GPU): `rollout eval --suite mmlu --checkpoint <snapshot-id> --dry-run` validates args, resolves the checkpoint to a ModelRef, and exits 0 WITHOUT constructing a live backend (mirrors infer batch --dry-run).
    - Test (help parses): `rollout eval --help` lists `--suite`, `--checkpoint`, `--config`, `--dry-run`, `--format`.
    - Test (dispatch): with the `test-mock-backend` feature + HF_OFFLINE=1, `rollout eval --suite gsm8k --checkpoint <id>` runs the eval-as-job over the 10-row fixture against MockEvalBackend and emits per-task scores (json format).
    - Test (suite validation): an unknown `--suite` value is rejected with a clear error.
  </behavior>
  <action>
    Add `Eval(eval::EvalCmd)` to `enum Cmd` in `main.rs` (sibling to `Infer`/`Train`/`Snapshot`/`Cloud`) + the dispatch
    arm. Create `src/eval.rs` mirroring `infer.rs`: `#[derive(Args)] EvalCmd` with
    `--suite <mmlu|ifeval|gsm8k>` (clap `ValueEnum`), `--checkpoint <snapshot-id>`, `--config <toml>` (optional),
    `--dry-run`, `--format <json>`. Resolve `--checkpoint` via the existing `SnapshotId`→`Snapshot.parts[]` path
    (spec 04): the weights blob's `ContentId` populates `ModelRef.content_id`; build the `ModelRef`. `--dry-run`
    short-circuits before backend construction (works with neither backend feature). Backend selection feature-gated
    exactly like infer batch (`test-mock-backend` for the GPU-free path; `vllm` for live). Dispatch to
    `rollout_harness_eval`'s `EvalHarness::run(model, ctx)` and print the `EvalReport` (json). Add
    `rollout-harness-eval = { path = "../rollout-harness-eval" }` to `crates/rollout-cli/Cargo.toml` + the
    `test-mock-backend` feature wiring.

    Reconcile `docs/specs/08-cli.md`: in §"rollout infer <mode>" REMOVE the `rollout infer eval --config ...` line;
    add a top-level `### rollout eval` subcommand entry under §2 Subcommands with the example
    `rollout eval --suite mmlu --checkpoint <snapshot-id>`. Leave the `rollout plan` "Harness DAG: acyclic, 3 nodes"
    line with a footnote/TODO noting it is a v1.2 concern (D-CORE-02) — do NOT implement DAG validation.
  </action>
  <verify>
    <automated>cargo test -p rollout-cli --features test-mock-backend eval --tests &amp;&amp; cargo build -p rollout-cli &amp;&amp; rg -q "rollout eval" docs/specs/08-cli.md &amp;&amp; ! rg -q "rollout infer eval" docs/specs/08-cli.md &amp;&amp; cargo clippy -p rollout-cli --all-targets -- -D warnings &amp;&amp; RUSTDOCFLAGS="-D warnings -D rustdoc::broken_intra_doc_links -D rustdoc::missing_crate_level_docs" cargo doc -p rollout-cli --no-deps --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Eval\(" crates/rollout-cli/src/main.rs` matches (the new enum arm).
    - `crates/rollout-cli/src/eval.rs` contains `suite` AND `checkpoint` AND `dry_run`.
    - `rg "EvalHarness|harness_eval" crates/rollout-cli/src/eval.rs` matches (dispatch to the eval crate).
    - `rg "rollout eval" docs/specs/08-cli.md` matches AND `rg "rollout infer eval" docs/specs/08-cli.md` returns NOTHING (D-EVAL-02 reconciliation).
    - `cargo test -p rollout-cli --features test-mock-backend eval` exits 0 (dry-run + help-parse + dispatch + suite-validation).
    - `cargo build -p rollout-cli` (no backend feature) exits 0 (dry-run path works without a backend).
    - The RUSTDOCFLAGS-deny `cargo doc -p rollout-cli` command exits 0 (DOCS-03 — the new EvalCmd public surface is documented).
  </acceptance_criteria>
  <done>`rollout eval` dispatches to the eval harness with checkpoint→ModelRef resolution + dry-run; spec 08 reconciled to the top-level form.</done>
</task>

<task type="auto">
  <name>Task 2: mdBook Harnesses chapter + SC4 workspace + 14-invariant verification</name>
  <files>docs/book/src/SUMMARY.md, docs/book/src/harnesses/index.md, docs/book/src/harnesses/env.md, docs/book/src/harnesses/tool-sandbox.md, docs/book/src/harnesses/eval.md, docs/book/src/harnesses/cli.md</files>
  <read_first>
    - docs/book/src/SUMMARY.md (the chapter structure to extend — add a Harnesses section after Distribution, mirroring the Inference/Training/Cloud chapter layout)
    - .planning/phases/07-harnesses-env-tool-eval/07-RESEARCH.md "Honest sandbox-depth matrix" + the eval scoring conventions + the env reward-via-plugin pattern (source material for the chapters)
    - crates/rollout-harness-tool/README.md (07-04 — the sandbox-depth matrix to summarize in tool-sandbox.md, single source of truth)
    - .planning/ROADMAP.md §"Phase 7" Success Criteria #4 (the SC4 gate: 5 new crates green + dep-direction at 14 invariants)
    - crates/rollout-core/tests/dependency_direction.rs (the 14-invariant lint to assert green — count == 14, names present)
  </read_first>
  <action>
    Add a "Harnesses" section to `docs/book/src/SUMMARY.md` (after Distribution) with sub-pages: index, env, tool-sandbox,
    eval, CLI. Write each chapter:
    - `index.md`: the three-trait rationale (spec 07 §1), the batched principle, the algo-layer dep-direction note, the
      "every harness testable locally without cloud creds/GPU" constraint.
    - `env.md`: HARNESS-01 — reset/step/close, multi-turn (D-ENV-01), reward via plugin host (D-ENV-03), no ObjectStore
      persistence in v1.1 (D-ENV-02), the `env_deterministic_replay` witness.
    - `tool-sandbox.md`: HARNESS-02 — the layered defense, the curated seccomp allowlist, the kernel ≥5.13 fail-closed
      gate, the SSRF connector, the macOS dev stub, and the honest threat-model boundary (summarize the 07-04 README
      matrix; state "process-isolated, NOT VM-isolated; NOT a security perimeter for actively malicious code").
    - `eval.md`: HARNESS-03 — MMLU acc/acc_norm, IFEval strict (language constraints skipped), GSM8K ####, the pinned
      lm-eval version, offline-default fixtures (HF_OFFLINE=1), eval-as-WorkQueue-job (D-EVAL-05).
    - `cli.md`: `rollout eval --suite ... --checkpoint ...` surface + the spec-08 reconciliation note.

    Then VERIFY the SC4 gate (do NOT edit the lint — it already has 14 invariants + the harness names; 07-00 made the
    crates physical): run `cargo test --workspace --tests` (5 new crates: rollout-cloud-aws + rollout-cloud-gcp from
    Phase 5, rollout-harness-text + rollout-harness-tool + rollout-harness-eval new here) and
    `cargo test -p rollout-core dep_direction_invariants_hold`. Confirm the invariant comment in
    `dependency_direction.rs` documents 14 invariants. One-line comments; DOCS-02 satisfied (this commit touches docs).
  </action>
  <verify>
    <automated>mdbook build docs/book &amp;&amp; cargo test --workspace --tests &amp;&amp; cargo test -p rollout-core dep_direction_invariants_hold &amp;&amp; cargo xtask schema-gen --out-dir /tmp/sg-check &amp;&amp; cargo deny check &amp;&amp; RUSTDOCFLAGS="-D warnings -D rustdoc::broken_intra_doc_links -D rustdoc::missing_crate_level_docs" cargo doc --workspace --no-deps --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `docs/book/src/SUMMARY.md` contains `Harnesses` AND links to `harnesses/tool-sandbox.md`; `mdbook build docs/book` exits 0.
    - `docs/book/src/harnesses/tool-sandbox.md` contains `seccomp` AND `NOT VM-isolated`; `docs/book/src/harnesses/eval.md` contains `acc_norm` AND `HF_OFFLINE`.
    - `cargo test --workspace --tests` exits 0 (5 new crates present and green — SC4).
    - `cargo test -p rollout-core dep_direction_invariants_hold` exits 0 AND `rg -c "invariant" crates/rollout-core/tests/dependency_direction.rs` confirms the 14-invariant documentation is intact.
    - `cargo deny check` exits 0; `cargo xtask schema-gen` produces no drift against the committed schema (RunConfig unchanged — no HarnessNode added per D-CORE-02).
    - The workspace-wide RUSTDOCFLAGS-deny `cargo doc --workspace --no-deps --all-features` exits 0 (DOCS-03 phase closeout gate — completes the §9.3 contract across all 5 crates).
  </acceptance_criteria>
  <done>The mdBook Harnesses chapter ships and builds; the SC4 workspace-count + 14-invariant gate is green; schema-drift stays clean.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace --tests` green (macOS lane: compile + stub + offline + in-process SSRF; the Linux lane additionally runs the cfg(linux) sandbox enforcement witnesses).
- `cargo test -p rollout-core dep_direction_invariants_hold` green (14 invariants, 5 new crates present — SC4).
- `mdbook build docs/book` green; `cargo deny check` + `cargo xtask schema-gen` drift green.
- `docs/specs/08-cli.md` reconciled; `rollout eval` works dry-run with no backend.
</verification>

<success_criteria>
- `rollout eval --suite mmlu --checkpoint <id>` produces per-task scores (ROADMAP SC3 command); spec 08 reconciled (D-EVAL-02).
- mdBook Harnesses chapter (env, tool-sandbox, eval, CLI) ships + builds.
- SC4: `cargo test --workspace --tests` green with 5 new crates; dep-direction lint at 14 invariants.
- No HarnessGraph/DAG validation added (D-CORE-02); schema-drift clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-harnesses-env-tool-eval/07-05-SUMMARY.md` recording: the final
`rollout eval` flag surface, the spec-08 edit, the checkpoint→ModelRef resolution path, the mdBook chapters added,
and the verified workspace crate count + invariant count for the phase-verification record.
</output>
