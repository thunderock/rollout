# Deferred items — Phase 07

- **ROADMAP.md Progress table is stale for all v1.1 phases.** `roadmap update-plan-progress 07` reports `summary_count: 0` despite 07-00..07-04 SUMMARY.md files existing on disk; the row stays `0/5 / Planned`. Phases 5 and 6 likewise show `0/0 / Not started` though both are complete. This is a pre-existing roadmap-tooling counting quirk (not introduced by 07-04) — the per-plan SUMMARY/STATE rollup is accurate; only the ROADMAP Progress table aggregation is off. Leave for the phase-closeout (07-05) / orchestrator to reconcile rather than hand-editing fabricated counts.

## Discovered during 07-05 (closeout)

- **`cargo doc --workspace --no-deps --all-features` fails on `h3-quinn 0.0.7` (pre-existing, upstream).** `--all-features` enables `rollout-transport`'s EXPERIMENTAL `quic` stretch feature → pulls `h3-quinn 0.0.7`, which accesses `quinn::StreamId.0` (now a private field) → `E0616`. Reproduced on the pre-Phase-7 tree, so it is independent of Phase 7. The authoritative DOCS-03 gate as CI enforces it (`.github/workflows/ci.yml` ~L211) runs `cargo doc --workspace --no-deps` WITHOUT `--all-features` and is green (exit 0) across all 5 new crates. Fix (deferred): bump quinn/h3-quinn/tonic-h3 to a compatible cohort when the experimental QUIC transport is actually pursued (post-v1.1); changing an unused experimental feature's deps is out of scope for the closeout.

- **`restart_no_duplicates.rs` clippy lints under `--features test-mock-backend` (pre-existing, plan 03-05).** `crates/rollout-cli/tests/restart_no_duplicates.rs` trips `clippy::format_push_string` + `clippy::too_many_lines` under `-D warnings` when `test-mock-backend` is on. Unrelated to the 07-05 eval CLI work (file not touched by Phase 7). Not in the Phase-7 verify path: Task-1 clippy is `-p rollout-cli --all-targets` (no feature) → green; SC4 `cargo test --workspace --tests` (bare) does not compile this test. Fix (deferred): clean up in a 03-05 follow-up.
