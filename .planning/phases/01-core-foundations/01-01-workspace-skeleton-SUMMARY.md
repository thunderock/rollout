---
phase: 01-core-foundations
plan: 01
subsystem: infra
tags: [cargo, rust, workspace, xtask, clap, toolchain]

requires: []
provides:
  - Cargo workspace root (members + workspace.dependencies pins)
  - Pinned Rust toolchain (1.88.0 + rustfmt + clippy)
  - `cargo xtask` alias via .cargo/config.toml
  - rollout-core empty-but-compiles library skeleton
  - rollout-cli binary (`rollout`) with stub `schema` subcommand
  - xtask binary with stub schema-gen / check-deps dispatcher
  - Cargo.lock committed (workspace with binaries)
affects: [01-03-rollout-core-content, 01-04-schema-gen-pipeline, 01-05-dep-direction, 01-06-github-actions-ci, 01-07-docs-site]

tech-stack:
  added:
    - serde 1 (derive)
    - serde_json 1
    - schemars 1.2.1
    - thiserror 2.0.18
    - async-trait 0.1.89
    - tracing 0.1
    - ulid 1.2.1 (serde)
    - blake3 1.8.5
    - clap 4 (derive)
    - cargo_metadata 0.18
  patterns:
    - workspace.dependencies single-source pinning
    - members inherit workspace.package via `*.workspace = true`
    - xtask crate is publish=false, does NOT inherit workspace.package
    - cargo alias indirection (`cargo xtask` -> `run --package xtask --`)

key-files:
  created:
    - Cargo.toml
    - rust-toolchain.toml
    - .cargo/config.toml
    - Cargo.lock
    - crates/rollout-core/Cargo.toml
    - crates/rollout-core/src/lib.rs
    - crates/rollout-core/README.md
    - crates/rollout-cli/Cargo.toml
    - crates/rollout-cli/src/main.rs
    - crates/rollout-cli/README.md
    - xtask/Cargo.toml
    - xtask/src/main.rs
  modified:
    - .gitignore

key-decisions:
  - "resolver = \"2\" kept explicit alongside edition 2021 (per RESEARCH.md note)."
  - "tracing included in workspace.dependencies even though no crate uses it yet — single workspace pin per RESEARCH.md guidance; rollout-core will re-export in plan 01-03."
  - "Cargo.lock committed (workspace with binary crates); .gitignore left without Cargo.lock entry."
  - "xtask pinned to its own edition/version (publish=false), does not inherit workspace.package, per RESEARCH.md §Pattern 5."
  - "rollout-cli forbid(unsafe_code) at binary level even though workspace.lints.rust already declares it — defensive duplication; cheap."

patterns-established:
  - "Workspace inheritance: per-crate manifests use `*.workspace = true` for version/edition/license/rust-version/repository."
  - "Workspace lints inherited via `[lints] workspace = true`."
  - "xtask alias resolves via `.cargo/config.toml`, not Makefile; Makefile (plan 01-02) wraps the alias."
  - "Stub subcommands print a message that names the future plan that wires them (e.g., `(plan 04)`), so anyone running the stub can find the next step."

requirements-completed: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05]

duration: 2min
completed: 2026-05-19
---

# Phase 01 Plan 01: Workspace Skeleton Summary

**Cargo workspace bootstrapped with three crates (`rollout-core`, `rollout-cli`, `xtask`), pinned 1.88.0 toolchain, `cargo xtask` alias wired, and all 10 workspace dependency versions locked per RESEARCH.md.**

## Performance

- **Duration:** ~2 min (104s wall, two tasks, single-process)
- **Started:** 2026-05-19T22:18:58Z
- **Completed:** 2026-05-19T22:20:42Z
- **Tasks:** 2
- **Files modified:** 13 (12 created, 1 edited)

## Accomplishments

- Workspace root manifest with `[workspace.dependencies]` pinning all 10 crates from RESEARCH.md §Standard Stack.
- Pinned Rust toolchain 1.88.0 with `rustfmt` + `clippy` components.
- `cargo xtask` alias resolves through `.cargo/config.toml`.
- Three crates compile cleanly under `cargo build --workspace` (and `cargo check --workspace`).
- `cargo xtask schema-gen` runs and prints `"schema-gen: not yet implemented (plan 04)"` (exit 0).
- `cargo run -p rollout-cli -- schema --format json` runs and prints `"schema subcommand not yet wired (plan 04)"` (exit 0).
- `cargo fmt --all -- --check` passes.

## Task Commits

1. **Task 1: Workspace root files** — `8e727a6` (feat)
2. **Task 2: rollout-core + rollout-cli + xtask skeletons** — `067be34` (feat)

**Plan metadata commit:** appended after this SUMMARY is written.

## Files Created/Modified

- `Cargo.toml` — workspace root: members, resolver, workspace.package, workspace.lints, workspace.dependencies.
- `rust-toolchain.toml` — pin channel 1.88.0 + rustfmt + clippy.
- `.cargo/config.toml` — `xtask = "run --package xtask --"` alias.
- `.gitignore` — added `.envrc.local` (existing entries already covered target/, .DS_Store, *.swp).
- `Cargo.lock` — generated by `cargo build --workspace`, committed (workspace with binaries).
- `crates/rollout-core/Cargo.toml` — inherits workspace.package, declares all 8 workspace deps.
- `crates/rollout-core/src/lib.rs` — crate-level doc comment + `#![forbid(unsafe_code)]`.
- `crates/rollout-core/README.md` — Layer 0 description + usage stub.
- `crates/rollout-cli/Cargo.toml` — `[[bin]] name = "rollout"`, depends on rollout-core + clap + serde_json.
- `crates/rollout-cli/src/main.rs` — clap `Cli`/`Cmd` with `Schema { format }` subcommand printing stub.
- `crates/rollout-cli/README.md` — CLI status note + reference to docs/specs/08-cli.md.
- `xtask/Cargo.toml` — `publish = false`, version 0.0.0, edition 2021, deps from workspace.
- `xtask/src/main.rs` — argv-based dispatcher for `schema-gen` and `check-deps` stubs.

## Decisions Made

All decisions are pre-locked in `01-CONTEXT.md` (D-CRATE-01, D-CRATE-02, D-CFG-02, D-CLI-01). The plan exercised one "Claude's Discretion" point from the CONTEXT:

- **Workspace includes `tracing` as a workspace dependency** — recommended by RESEARCH.md §Standard Stack and §Claude's Discretion: "Whether `tracing` is re-exported from `rollout-core` (recommended, single version pin for the workspace)." Chosen: yes. Pin is `0.1` (semver-compatible with any 0.1.x). rollout-core declares it but does not yet `pub use` it; that wiring lands in plan 01-03.

No other deviations from the plan.

## Deviations from Plan

None — plan executed exactly as written, including the inline code samples in `<action>` for both tasks. All 24 `<acceptance_criteria>` checks (13 for Task 1, 11 for Task 2) passed.

## Issues Encountered

- **`missing_docs` warning on rollout-cli binary.** The workspace-level `[workspace.lints.rust] missing_docs = "warn"` triggers on the rollout-cli `main.rs` since it has no crate-level `//!` doc. **Resolution:** Left as a warning (not an error). Plan 01-07 (mdBook docs / DOCS-03 rustdoc gate) explicitly owns adding crate-level doc comments per the orchestrator's important_notes: *"Plan 01-07 will run after you and 01-02 — do NOT touch docs/book/ or seed doc comments for rollout-cli/xtask main.rs."* The rustdoc gate that escalates this to `-D warnings` lands in plan 01-06's CI.

## User Setup Required

None — no external service configuration required for this plan. Subsequent plans pull in `mdbook`, `convco`, `datamodel-code-generator`, `check-jsonschema`, etc. as they wire schema-gen and CI.

## Next Phase Readiness

- **Ready for plan 01-03 (rollout-core content):** workspace.dependencies are pinned, rollout-core crate exists empty-but-compiles, `src/lib.rs` is ready to grow `src/traits/`, `src/errors.rs`, `src/ids.rs`, `src/config/`, `src/events.rs`.
- **Ready for plan 01-04 (schema-gen pipeline):** xtask binary exists with a `schema-gen` stub branch; plan 01-04 just replaces the `eprintln!` with the real `schemars::schema_for!` call.
- **Ready for plan 01-05 (dep-direction & cargo-deny):** cargo_metadata 0.18 is already pinned in workspace.dependencies, available for the architecture-lint test.
- **Ready for plan 01-06 (CI):** Cargo.lock is committed (deterministic CI builds); toolchain is pinned (CI consumes rust-toolchain.toml).
- **Ready for plan 01-07 (docs site):** rollout-cli/xtask main.rs are intentionally minimal so 01-07 can add the crate-level doc comments without conflict.

No blockers. No concerns.

---
*Phase: 01-core-foundations*
*Completed: 2026-05-19*

## Self-Check: PASSED

Files verified:
- FOUND: Cargo.toml
- FOUND: rust-toolchain.toml
- FOUND: .cargo/config.toml
- FOUND: Cargo.lock
- FOUND: crates/rollout-core/Cargo.toml
- FOUND: crates/rollout-core/src/lib.rs
- FOUND: crates/rollout-core/README.md
- FOUND: crates/rollout-cli/Cargo.toml
- FOUND: crates/rollout-cli/src/main.rs
- FOUND: crates/rollout-cli/README.md
- FOUND: xtask/Cargo.toml
- FOUND: xtask/src/main.rs

Commits verified:
- FOUND: 8e727a6 (Task 1)
- FOUND: 067be34 (Task 2)
